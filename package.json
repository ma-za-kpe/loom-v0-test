{
  "dependencies": {
    "@loom/auth-core": "2.1.3",
    "@loom/stripe-shim": "1.2.2",
    "@loom/webhook-verifier": "file:packages/loom-webhook-verifier-1.0.0.tgz"
  },
  "description": "Acme Corp billing service — test repo for Loom V0 E2E workflow",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  },
  "engines": {
    "node": "\u003e=22.0.0"
  },
  "main": "dist/index.js",
  "name": "acme-billing-service",
  "private": true,
  "scripts": {
    "build": "npx tsc",
    "start": "node dist/server.js",
    "dev": "npx tsx src/server.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "type": "module",
  "version": "1.0.0"
}
